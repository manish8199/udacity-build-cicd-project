name: frontend ci 

on:
  workflow_dispatch:

  push:
    branches:
      -main

  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4.1.7
            

    - name: setup node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18
        cache: 'npm'
        cache-dependency-path: "./starter/frontend/package-lock.json"

    - name: install depedencies
      run: cd starter/fronend && npm ci

    - name: run lint
      run: cd starter/fronend && npm run lint

    - name: print message
      run: echo "lint build is running"

  test:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v4.1.7

    - name: setup node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18
        cache: 'npm'
        cache-dependency-path: "./starter/frontend/package-lock.json"

    - name: install depedencies
      run: cd starter/fronend &&  npm ci

    - name: run tests
      run: cd starter/fronend && npm run test

    - name: print message
      run: echo "test build is running"

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]


    steps:
    - name: checkout code
      uses: actions/checkout@v4.1.7

    - name: setup node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: install depedencies
      run: npm ci

    - name: run tests
      run: npm run test

    - name: print message
      run: echo "test build is running"

